//! Navigation helpers (re-exported from state for module structure).
//!
//! All navigation logic lives in `TreeState` methods. This module exists
//! to match the crate's declared module layout and can hold future
//! standalone navigation utilities if needed.

// Navigation is implemented directly on TreeState in state.rs.
// This module reserved for pure helper functions if they arise.
