[package]
name = "rat-canvas"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Infinite canvas viewport with pan/zoom and coordinate mapping"

[features]
default = ["serde"]
serde = ["dep:serde"]

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }